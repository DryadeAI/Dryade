# data-processor - Custom MCP-native Agent Configuration
# Auto-generated by Dryade Agent Factory

name: data-processor
version: "1.0.0"
description: "A simple data processing agent that can transform CSV data"

# Framework type - MCP native (no external framework)
framework: custom

# Required MCP servers
mcp_servers:

# Capabilities exposed by this agent
capabilities:
  []

# Task routing keywords
routing:

# Default context values
defaults:
  timeout: 30
