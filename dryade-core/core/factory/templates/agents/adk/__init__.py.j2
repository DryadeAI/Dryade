"""{{ name }} - ADK Agent.

{{ description }}

Auto-generated by Dryade Agent Factory.
"""

from __future__ import annotations

import logging
from pathlib import Path
from typing import Any

import yaml

from core.mcp.tool_wrapper import MCPToolWrapper

logger = logging.getLogger(__name__)


# ---------------------------------------------------------------------------
# Configuration
# ---------------------------------------------------------------------------


def _load_config() -> dict[str, Any]:
    """Load agent configuration from YAML file."""
    config_path = Path(__file__).parent / "config.yaml"
    if config_path.exists():
        with open(config_path) as f:
            return yaml.safe_load(f)
    return {}


# ---------------------------------------------------------------------------
# MCP Tool Wrappers
# ---------------------------------------------------------------------------

{% for tool in tools %}
_{{ tool.tool }}_wrapper = MCPToolWrapper("{{ tool.server }}", "{{ tool.tool }}", "{{ tool.description }}")
{% endfor %}

# ---------------------------------------------------------------------------
# ADK Tool Adapters
# ---------------------------------------------------------------------------


def _create_adk_tools() -> list:
    """Create ADK FunctionTool wrappers for each MCP tool.

    Returns:
        List of ADK FunctionTool instances, or empty list if ADK not installed.
    """
    try:
        from google.adk.tools import FunctionTool
    except ImportError:
        logger.warning("google-adk not installed; returning empty tool list")
        return []

    tools = []
{% for tool in tools %}

    def _invoke_{{ tool.tool }}(**kwargs: Any) -> str:
        """{{ tool.description }}"""
        return _{{ tool.tool }}_wrapper.call(**kwargs)

    tools.append(FunctionTool(_invoke_{{ tool.tool }}))
{% endfor %}

    return tools


# ---------------------------------------------------------------------------
# Factory function (discovered by zero_dev.py _wrap_adk)
# ---------------------------------------------------------------------------

root_agent = None


def create_agent(config: dict[str, Any] | None = None) -> Any:
    """Create and return an ADK LlmAgent.

    Args:
        config: Optional configuration override.

    Returns:
        google.adk.agents.LlmAgent instance.
    """
    global root_agent

    try:
        from google.adk.agents import LlmAgent
    except ImportError as exc:
        raise ImportError(
            "google-adk is required for ADK agents. "
            "Install with: pip install google-adk"
        ) from exc

    cfg = config or _load_config()
    tools = _create_adk_tools()

    # Model can be configured via Dryade's LLM settings
    agent = LlmAgent(
        model=cfg.get("model", "{{ model | default('gemini-2.0-flash') }}"),
        name="{{ name }}",
        instruction="{{ instruction }}",
        tools=tools,
    )

    root_agent = agent
    return agent


async def run(task: str, **kwargs) -> str:
    """Entry point declared in dryade.json run_function."""
    agent = create_agent()
    try:
        from google.adk.runners import Runner
        from google.adk.sessions import InMemorySessionService
        runner = Runner(agent=agent, app_name="{{ name }}", session_service=InMemorySessionService())
        session = await runner.session_service.create_session(app_name="{{ name }}", user_id="factory")
        from google.genai import types
        content = types.Content(role="user", parts=[types.Part(text=task)])
        response_parts = []
        async for event in runner.run(user_id="factory", session_id=session.id, new_message=content):
            if event.is_final_response() and event.content and event.content.parts:
                response_parts.extend(p.text for p in event.content.parts if p.text)
        return "".join(response_parts) if response_parts else ""
    except ImportError:
        raise RuntimeError("google-adk is required. Install with: pip install google-adk")


__all__ = ["create_agent", "run"]
